# DataTide — application layer (`app/`)

This folder holds **Python** entrypoints for future serving (FastAPI, batch jobs, etc.). See `main.py`.

The **React web UI** lives in **`site/`** (Vite + TypeScript + MapLibre). From the repo root:

```bash
cd site
npm install
npm run dev
```
