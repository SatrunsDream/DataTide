# Pin versions as the project stabilizes.
numpy>=1.24
pandas>=2.0
pyarrow>=14.0
netcdf4>=1.6
xarray>=2023.1
pyyaml>=6.0
python-dotenv>=1.0
jupyter>=1.0
pytest>=7.0

# Modeling stack. JAX keeps removing private APIs that old NumPyro pinned
# against (e.g. `xla_pmap_p` was dropped from jax.extend.core.primitives in
# jax 0.7), so keep these two in lockstep. numpyro>=0.20 requires jax>=0.7.
jax>=0.7
jaxlib>=0.7
numpyro>=0.20
arviz>=0.18
scipy>=1.11
matplotlib>=3.7
xgboost>=2.0
scikit-learn>=1.3

# Optional for app deployment (uncomment as needed)
# fastapi>=0.100
# uvicorn[standard]>=0.22
# streamlit>=1.28
